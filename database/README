This folder contains sample setup files for pgbouncer, SQL files to auto-create the database, and a context.xml file to define a data source in tomcat.  You'll need to define a password for each of them.
